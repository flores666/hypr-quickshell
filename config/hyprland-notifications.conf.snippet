# Notification layer rules.
# Compositor blur is disabled for stability on Hyprland 0.55.x.
# The crash reports point to CSurfacePassElement::needsLiveBlur()/CMonitor::useFP16().

layerrule = ignore_alpha 0.2, match:namespace notifications
